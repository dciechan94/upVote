/*
 * ManagePollsPage Messages
 *
 * This contains all the text for the ManagePollsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ManagePollsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ManagePollsPage container!',
  },
});
