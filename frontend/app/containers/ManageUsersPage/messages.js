/*
 * ManageUsersPage Messages
 *
 * This contains all the text for the ManageUsersPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ManageUsersPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ManageUsersPage container!',
  },
});
