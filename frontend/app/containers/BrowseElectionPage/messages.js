/*
 * BrowseElectionPage Messages
 *
 * This contains all the text for the BrowseElectionPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BrowseElectionPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BrowseElectionPage container!',
  },
});
