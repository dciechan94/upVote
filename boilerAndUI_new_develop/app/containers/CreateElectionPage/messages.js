/*
 * CreateElectionPage Messages
 *
 * This contains all the text for the CreateElectionPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CreateElectionPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CreateElectionPage container!',
  },
});
